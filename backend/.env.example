IFOOD_CLIENT_ID=
IFOOD_CLIENT_SECRET=

# Billing: no payment provider is integrated right now. Checkout answers HTTP 503 until
# one is — the next integration adds its own credentials here.
# See .claude/docs/integrations/BILLING_PROVIDER.md

# Base URL used in checkout return/completion redirects
# (dev default: http://localhost:5173)
APP_FRONTEND_BASE_URL=
